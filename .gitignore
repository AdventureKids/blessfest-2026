# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor / OS junk
*~
*.swp
.vscode/
.idea/

# Node (only if you add tooling later)
node_modules/
npm-debug.log*

# Build zips and archives (we don't want these in history)
*.zip

# Large media originals — exceed GitHub's 100 MB file limit; the live promo is on YouTube
media-source/
*.mp4

# Wrangler / Cloudflare local state
.wrangler/
.dev.vars
